import { ComingSoonPage } from "@/components/dashboard/coming-soon-page";

export default function ScanPage() {
  return (
    <ComingSoonPage
      label="Trace a leak"
      title="Scan"
      description="Inspect a suspected copy and uncover the source behind a leak."
      panelDescription="Leak scanning is coming soon. This area will guide you through checking a file and reviewing its attribution result."
    />
  );
}
